function addition(value: string) {//defining the function 'addition' and passing a string parameter through it called value
    console.log("Value is: " + value);//outputting what the value of the variable value is via console.log
    }
    let firstVal: number = 42;//Declaring the variable firstVal and assigning a value to it
    let secondVal: number = 1;//Declaring the variable secondVal and assigning a value to it
    let sumOfVals: string = (firstVal + secondVal).toLocaleString();//Crreated a variable named sumOfVals which adds the two variables and outputs it as a string
    
    addition(sumOfVals);//Calls the addition function and puts the variable sumOfVals through it
    